// ES6 Destructuring
// Converting array.txt or object to variable is called destructuring

/*
// Array Destructuring

let arr = [1, 2, 3];

// JS code

var a = arr[0];
var c = arr[2];

// ES6 Code

let [a, , c] = arr;

console.log(a, c);

// Obj Destructuring

let obj = {
    name : 'Kartic Gharami',
    email : 'dev.kartic@gmail.com'
}

// JS code

var name = obj.name;
var email = obj.email;

// ES6 Code

let { name, email } = obj;

console.log(name, email)


*/

/*

let obj = {
    name : 'Kartic Gharami',
    email : 'dev.kartic@gmail.com'
}
let { name, email } = obj;

// If parameter as variable then ordering must be same
// function print(name, email){
//     console.log(`Hellow ${name}, send mail to ${email}`);
// }
// print(name, email);


// If parameter as obj then indexing autometically

function print({name, email}){
    console.log(`Hellow ${name}, send mail to ${email}`);
}
print({email, name});

*/



